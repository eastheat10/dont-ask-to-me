import { collection, getDocs } from "firebase/firestore";
import { db } from "../firestore/index.js";

// export async function tmp() {
//   const item = await getDocs(collection(db, "question"));

//   console.log(item);
// }

console.log(collection);
